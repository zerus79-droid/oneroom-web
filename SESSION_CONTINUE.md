# oneroom-web · 이어하기

**다른 AI / 새 세션은 `AGENTS.md`만 읽으면 됨.**

- 폴더: `C:\Users\someb\oneroom-web`
- 실행: `start.bat` 또는 `python app.py` → http://127.0.0.1:5000
- 구조 분리 끝. 다음 작업은 주인 지시.

```bat
cd /d C:\Users\someb\oneroom-web
"%USERPROFILE%\.grok\bin\grok.exe" -c
```

# 다른 AI 작업 인수인계

이 문서는 작업 중인 AI가 바뀌어도 현재 상태와 변경 이유를 빠르게 파악하기 위한 기록이다.

## 2026-09-15: 2번 완료 — 현재계산 저장과 저장본 금액 고정

- 사용자 확정: 현재계산은 계속 저장할 수 있다. 저장본은 최신 수금·계약 변경으로 바뀌지 않고, 사용자가 다시 저장할 때만 교체한다. 버전별 영구 이력 기능을 추가한 것은 아니다. 기존 덮어쓰기 확인은 유지한다.
- `jungsan.py`: `jungsan_m.snapshot_json`에 version=1과 화면/인쇄용 data 전체를 저장한다. 기존 m/det 호환 필드도 같은 연결·잠금·트랜잭션에서 저장한다. JSON 형식/주소/월/필수 구조는 상세 삭제 전에 검사한다. 컬럼 미적용 DB는 DML 전에 안내하고 거부한다. 조회 시 유효한 JSON을 먼저 반환하여 현재 건물/계약/수금/미수/조정/수리 자료 및 계산 엔진을 거치지 않는다. 손상 또는 미지원 형식은 오류로 알리며 현재계산으로 몰래 대체하지 않는다.
- 옛 저장본(NULL): jungsan_m/det에 남은 값만 표시한다. 원래 없던 호실별 입금액, 선불/후불, 당시 통장 구분, 대체액, 지불수리비, 상세 내역은 현재 자료로 복원하지 않는다. 합계는 저장 헤더 기준이며 계약금액/미수는 저장 상세 기준이다. 옛 관리비 복사 오류도 자동 보정하지 않는다. 방향 근거가 없는 지급/청구 라벨은 `저장 정산액`, 통장 구분은 미기록으로 안내한다. 현재계산에서 검토하고 명시적으로 재저장하면 새 완전 저장본으로 바뀐다. 과거 자료를 일괄 backfill/재저장하지 말 것.
- `templates/jungsan.html`: 저장본의 대체·조정·관리지시 편집은 숨기고 현재계산으로 안내한다. 저장 버튼 자체는 기존처럼 권한이 있는 사용자에게 제공하며 기존 저장본이 있으면 덮어쓰기 확인 후 현재계산을 저장한다. 인쇄 클릭 JS가 기존 href의 src를 버리던 오류를 고쳐, 현재계산에서 인쇄하면 현재계산, 저장본에서 인쇄하면 저장본을 사용한다. **인쇄 템플릿 및 인쇄 CSS 규격은 수정하지 않았다.**
- 월별 목록: 저장된 pay_amt를 최신 통장 주체로 재계산하거나 다시 천원 절사하지 않는다. 전체 월 조회에서 큰 snapshot_json을 가져오지 않도록 필요한 합계 컬럼만 선택한다. 공용 수리 복사는 현재 페이지에서 저장본 없는 건물에만 수행한다. 건물명/건물 목록 메타는 기존 목록대로 bd01을 사용한다(금액에는 사용하지 않음).
- DB 적용: `005_jungsan_snapshot_json.sql`을 로컬 `sinbee.jungsan_m`에 적용했다. 기존 컬럼·인덱스·금액은 유지하고 nullable LONGTEXT 컬럼 하나만 추가(실제 적용은 ALGORITHM=INSTANT, LOCK=NONE, lock_wait_timeout=5). 새 컬럼은 기존 6,581행 모두 NULL이다. `bd01`, `jungsan_det`, 수금/퇴실 업무 자료에는 수정하지 않았다.
- 적용 전 백업: `backups/jungsan_before_snapshot_20260915.sql`, 1,272,575 bytes, SHA-256 `a1c82d0ef8a4b22367d4159726717ac48d3e47a3612c8153b5fc38d788f48f6f`. native mariadb-dump로 백업한 6,581행을 연결 한정 임시 테이블에 전부 복원해 원본과 비교했다. 적용 전후 원래 컬럼 전체 행의 정렬 해시: `9a0058ed802bff3b020f9315989993609e641b3d007d83299a4969e9782c0d98` 동일. 백업은 Git 제외이며 보관한다.
- 검증: `JUNGSAN_TXN_DB_TEST=1`로 `tests.test_jungsan_saved_view`, `tests.test_jungsan_snapshot`, `tests.test_jungsan_exit_vouchers`, `tests.test_tenant_form`, `tests.test_settlement_calendar` 총 63개 통과. 실제 임시 InnoDB 테이블에서 JSON+합계+상세 최초 저장/재저장/실패 복구까지 검증했다. 저장본/기본 조회에서 최신 DB·재계산 호출 차단, NULL 옛 자료, 잘못된 JSON, 음수/0원, 200자 관리지시 보존, 화면/인쇄 수치와 A/C 권한을 확인했다. 옛 헤더 `jungke_desc`는 varchar(50)이므로 호환 필드만 50자로 제한하며 전체 지시는 JSON에 남긴다.
- 업무 DB 쓰기를 막은 로그인 GET 스모크 14곳 및 저장본/현재계산 화면·인쇄 4곳 HTTP 200. 실제 2026-05 현재계산 508-88/203호 -206,667원, 1139-4/308호 130,700원 유지 및 전체 결과 JSON 왕복 검증. 월별 목록 3회 0.228/0.224/0.225초. 브라우저 도구가 `No browser is available`을 반환하여 실제 클릭 검증은 못 했고 동일 템플릿/요청 흐름 테스트로 대체했다. 업무 정산서는 저장/덮어쓰기하지 않았다.
- 3번 퇴실 예비 계산식·종류06 생성 연결은 여전히 미해결. 4번 테스트의 저장본 사례는 이제 당시 계산 결과를 저장한 JSON을 다시 읽도록 변경했다. 저장본에서 최신 종류06을 다시 조회해 0원/음수를 판정하지 않는다. 현재계산 엔진의 전표 존재 판정과 통장 분기는 그대로다.
- 인쇄·메뉴의 기존 사용자 승인 변경은 유지. 작업 브랜치 `fix/checkout-settlement`만 커밋/푸시 대상이며 master 병합·운영 서버 재시작은 별도로 하지 않았다. 검증용 임시 스크립트/가상자료 서버는 정리했으며 테스트 코드와 백업만 남긴다.

## 2026-09-15: 2·3·4번 정리 — 4번 전표 존재 판정 수정

- 범위: 4번의 종류06 존재 판정만 수정. 2번 저장본 금액 고정 기준은 사용자에게 확인 질문을 보냈고 아직 답변 전이다. 3번 신규 웹 퇴실과 월정산 연결은 읽기 전용으로 조사했다. 인쇄/메뉴와 실제 업무 DB 자료는 변경하지 않았다.
- 코드: `jungsan_engine._month_out_adjustment(pay_map=...)`는 `amt > 0` 대신 `06` 키 존재로 판단한다. `jungsan._jungsan_build_preview`의 현재계산/저장본 두 경로도 `exit_paid`의 truthiness 대신 전표 키를 검사하여 합계 0원 및 실입+대체 상계 0원을 보존한다. 합계 산식(실입+대체), 음수 부호, 전표가 없을 때의 기존 예비 계산, 건물주 통장 일할 경로는 바꾸지 않았다.
- 검증: `tests/test_jungsan_exit_vouchers.py` 6개를 먼저 추가했다. 수정 전에는 음수/0원 존재 불일치, 현재계산 0원/상계 0원 재계산, 저장본 상계 0원 대신 실입액 사용의 5개 subtest가 실패했다. 수정 후 신규 6개와 기존 47개(임시 InnoDB 실패 복구 opt-in 포함) 총 53개 통과. 필수 Python 컴파일·diff 검사 통과. 모든 신규 테스트의 업무 DB 읽기/쓰기는 mock 처리한다.
- 화면 확인: 일반 `db.execute`를 차단한 로그인 test client로 기본 GET 14곳 HTTP 200. 2026-05 현재계산 실제 조회에서 508-88/203호 입금액 -206,667원과 1139-4/308호 130,700원이 유지됐다. 실제 업무 정산서 저장/변경은 실행하지 않았다.
- 3번 추가 근거: 현재 `checkout._save_checkout`는 `bd07_out` INSERT/UPDATE와 `bd03_det` UPDATE만 한다. 두 테이블 및 `bd03_m`에 이를 대신하는 DB 트리거도 없었다. 즉 웹 신규 퇴실 때 월정산용 종류06 전표를 자동 생성하는 연결은 없다. XP 정적 분석 문서에는 종류06 생성/취소 흐름이 명시되어 있다. 단순히 세입자 미수 또는 일할 금액을 종류06에 복사하는 방식은 금지: 관리실 통장 선불은 건물주에게 이미 지급한 기간에 대한 별도 조정액이어야 한다.
- 실제 사례의 종류06(2026-05, 읽기 전용 확인): 508-88/203/17은 관리실 월세 통장(M), 선불(A), 실입 -206,667원·대체 0원 1건. 1139-4/308/07은 건물주 월세 통장(O), 후불(B), 실입 306,667원·대체 0원 1건이지만 현재 사용자 합의 경로는 퇴실 일할 130,700원을 사용한다. 후자의 저장본 분기는 아직 현재계산과 같은 통장 분기를 쓰지 않으므로 3번에서 따로 검증해야 한다. 4번 완료를 2·3번 완료로 보고하지 말 것.

## 2026-09-15: 1번 DB 적용 완료, 2·4·5번 확인 결과

- 사용자 승인 범위: 1번 DB 적용. 2번 배지 구분 확인, 4번 음수 판정 문제 위치 설명, 5번 기존 인쇄·메뉴 변경 내역 설명. 3번 퇴실 계산은 사용자가 점검 중이므로 수정하지 않는다.
- **DB 적용 완료**: 로컬 `sinbee.jungsan_m`, `sinbee.jungsan_det`만 MyISAM → InnoDB. 합계 6,581행·상세 124,669행. `bd01`은 MyISAM 그대로이며 업무 금액·행·컬럼·인덱스는 변경하지 않았다. 실행 로그 날짜는 2026-09-14, 이번 확인/정리는 09-15다.
- 백업: `backups/jungsan_before_innodb_20260914.sql` (21,117,514바이트, Git 제외, 개인정보 포함: 업로드/커밋 금지). SHA-256 `73259f5abea4a582dd1ce6ecc10336eebe3a91cf1fdb509212b6b01d90feb86f`. 이 파일은 보관하며 다시 실행한다고 덮어쓰지 말 것.
- 검증 절차: 두 원본 테이블 READ 잠금을 유지한 채 native mariadb-dump로 파일 백업. 파일을 연결 전용 TEMPORARY 테이블에 전체 복원하고 InnoDB 시험 전환 후 원본과 대조. 이후 원본 2개를 전환하고 모든 행의 정렬된 SHA-256, 전체 컬럼 정의, 인덱스 정의(엔진 변경 시 달라지는 cardinality 제외)를 비교해 모두 일치했다. 임시 복원 테이블은 연결 종료로 제거했다. 원본 ALTER 시간은 합계 약 0.5초·상세 약 5.5초(검사 포함)였다.
- 행 내용 비교 해시: `jungsan_m=80e8dce370d8db653016de41bf4823177803fa81238861515d10c6b6c7e35acf`, `jungsan_det=d1b4c179452e22217d8172fed30fec193c7226a1152042108578fe7e1983b2be`. 각 행을 컬럼명 정렬 JSON(default=str, ASCII, 공백 없음)으로 SHA-256한 bytes를 정렬·연결해 다시 SHA-256했다. 물리 행 순서 변경은 차이로 보지 않았다.
- 전환 후 `JUNGSAN_TXN_DB_TEST=1` 회귀 테스트 47개 재통과. 실제 업무 정산서 저장/덮어쓰기는 검증 목적으로 실행하지 않았으며 서버 재시작·master 병합도 하지 않았다. 1번 코드 `5cf924e`는 작업 브랜치에 있고 이제 로컬 DB의 엔진 검사도 통과할 조건이 갖춰졌다.
- **2번 정정**: 현재계산/저장본 배지는 이미 구현되어 있다(`templates/jungsan.html` 93행 부근). `1139-4 / 2025-07-31`의 실제 저장 자료로 로그인 test client에서 `src=saved/live` 전환과 HTTP 200을 확인했다(일반 DB 쓰기는 mock 차단). 같은 건물 2026-08에는 저장본이 없어 `src=saved` 요청도 현재계산으로 돌아간다. 남은 논점은 배지가 아니라 저장 당시 값을 그대로 표시할지다: 저장본 분기에서도 `bd03_det` 계약금액을 읽고, 월 수금·미수를 재계산한 뒤 공통 구간에서 입금합계/미수합계를 다시 만든다(`jungsan.py` 578·598·631·650·1163·1178행 부근). 조회가 저장 DB를 자동 덮어쓴다는 뜻은 아니다. 화면 하단도 “입금액은 해당 월 수금으로 보강”이라고 명시한다.
- **4번 정정**: 문제 위치는 `jungsan_engine.py:_month_out_adjustment` 370행 부근의 사전집계 `amt > 0`과 직접 조회 `COUNT(*) > 0` 불일치다. -206,667원/0원에 대해 전자는 False, 후자는 True로 재현했다. `jungsan.py` 833행의 `if exit_paid`는 음수를 True로 처리하므로 음수가 무조건 사라지는 버그라고 보고하면 안 된다. 0원·상계 합계 0원의 전표를 없는 것으로 처리해 재계산으로 넘어갈 수 있는 문제가 남아 있다. 수정은 하지 않았다.
- **5번 기존 변경 — 사용자 지시 확인, 유지**: 기준 `dc45749..f90f4a1`. `1e5fb71`에서 `templates/jungsan_print.html`의 @page 여백 12/12/14/12mm → 0, print .sheet padding 0/3/0/0 → 15/12/10/12mm, 쪽별 padding 복제, 표 머리 반복·행/하단 비용합계 블록 쪽 나눔 방지, 관리비 셀 우측→가운데 정렬, 지불수리비 표시를 추가했다. `b7284eb`는 관리지시 문구를 인쇄 하단에 추가했다. `f90f4a1`는 상단/홈 메뉴의 퇴실→수리 순서를 수리→퇴실로 바꾸고 홈 바로가기에서 수리와 퇴실의 위치도 교환했다. **2026-09-15 사용자가 이 변경들은 그록에게 직접 지시한 내용이라고 확인했다. 현재 방향을 유지하며 수정/원복 대상으로 취급하지 않는다.** 확인한 Git 커밋에는 변경 내용이 있지만 사용자 지시 배경은 명시되어 있지 않았고, 이 대화의 더 오래된 메뉴 요청만 근거로 보류 분류한 것은 잘못이었다. 해당 분류를 취소하고 TODO 5번을 완료 처리했다. 인쇄·메뉴 코드는 그대로 두고 문서만 정정했다.

## 2026-09-14: 순차 해결 1번 — 월정산 저장 안전성 (DB 전환 대기)

- 사용자 요청: 문제를 목록으로 관리하고 하나씩 해결. 우선 저장 중 자료 손실 문제만 보완한다. 퇴실 계산식, 인쇄 규격, 저장본 재계산은 후속 항목이다(`docs/TODO.md`).
- 확인: 현재 `fix/checkout-settlement`의 기존 변경은 5개 커밋(`21a0b05`~`f90f4a1`)으로 원격 브랜치에 존재하며 master는 `dc45749`다. 전체 master 반영은 보류 상태.
- DB에서 `information_schema.TABLES`를 읽어 확인한 결과 `jungsan_m`, `jungsan_det`, `bd01` 모두 MyISAM이다. 따라서 기존 코드에 begin/rollback만 추가하면 보호되지 않는다.
- 준비한 코드: `_jungsan_write_snapshot`이 두 정산 테이블의 InnoDB 여부를 먼저 검사한다. 아니면 데이터 변경 전에 저장을 거부한다. 건물·월별 `GET_LOCK`을 얻고 하나의 연결/트랜잭션으로 헤더 조회, 합계 저장, 상세 교체를 실행한다. 실패하면 rollback하고 잠금/연결을 해제한다. `/jungsan/save`는 실패 시 저장 성공 표시를 하지 않는다.
- 동시 저장 잠금은 이 웹 저장 함수를 사용하는 요청 사이에만 적용된다. 같은 잠금을 사용하지 않는 XP/직접 SQL의 동시 쓰기까지 막는 기능은 아니다.
- `004_jungsan_transaction_tables.sql`은 **아직 운영 DB에 실행하지 않았다**. 정산 테이블 2개만 전환한다. `bd01` 등 다른 테이블은 변환하지 않는다. 실제 전환 전까지 새 코드의 월정산 저장은 안내와 함께 거부되며 조회는 가능하다.
- 실제 전환 절차: (1) 웹·XP 등 DB 쓰기를 중단할 시간을 사용자에게 확인 (2) 두 테이블의 스키마/데이터를 `backups/` 등 Git 제외 위치에 파일로 백업하고 복원 가능 여부 확인(MyISAM이므로 `--single-transaction`만으로는 부족) (3) 전후 행수·합계 기준값 기록 (4) SQL 실행 (5) 두 엔진과 행수/합계/스키마를 비교하고 격리된 테스트 자료로 저장/실패 복구를 확인한 후 재개. 전환 중 잠금이 발생할 수 있고 ALTER 자체는 rollback되지 않는다. 중간 실패 시 부분 전환 상태에서 저장을 재개하지 말 것.
- 주의: 저장 중 rollback과 과거 저장 이력 보존은 별개다. 기존 저장본 조회가 최신 자료를 섞는 문제, 성공 저장 시 이전 이력을 남기는 문제는 2번에서 따로 해결해야 한다. 보호됐다고 보고하지 말 것.
- 검증: 필수 Python 컴파일 통과. `JUNGSAN_TXN_DB_TEST=1`로 `tests.test_jungsan_snapshot`, `tests.test_tenant_form`, `tests.test_settlement_calendar`를 함께 실행해 47개 통과. 새 테스트 8개는 같은 연결 사용, 전체 rollback, 신규 저장 실패, MyISAM/부분 전환 거부, 잠금 대기 실패, commit 실패, 실패 응답, 실제 임시 InnoDB 테이블의 합계·상세 복구를 확인한다. 임시 테이블은 별도 이름으로 빈 스키마만 복제하고 테스트 후 삭제하며 업무 자료는 수정하지 않는다.
- 화면 확인: 로그인 test client로 AGENTS.md의 GET 14곳 모두 HTTP 200. 일반 `db.execute` 쓰기와 POST 미리보기를 mock한 상태에서 실제 MyISAM 저장 거부도 확인(302, 성공 표시 없음). 실제 브라우저 입력 검증·운영 저장·서버 재시작은 하지 않았다.
- 실행 환경: `.venv`에는 `flask_caching` 등 일부 의존성이 없어 import 검증에 사용할 수 없었다. 기존 패키지가 설치된 `C:\Users\someb\AppData\Local\Programs\Python\Python312\python.exe`로 테스트했으며 패키지/환경 설정은 변경하지 않았다.

## 2026-09-10: 공용=수리공용, 수금 임+관, 월정산 대체 XP

- **공용**: `bd01_common_cost` 드롭. 마스터는 `bd05_suri` 호수 `공용` + `YEAR=1000`. 월정산/목록에서 그달 말일 행이 없으면 복사. 수리 합·목록은 `YEAR(suri_dt)>1000`.
- **수금**: 목록·인쇄·엑셀 헤더를 XP처럼. 종류 01은 `rent_manage_disp`=실입+대체 전액. `_split_char01_payment`는 테스트만 남김(화면 미사용).
- **월정산**: 책임관리 입금액 due=월세. 대체 호 `(대체)`. `imdae_dache = misu_tot`. 대체 버튼 대상은 여전히 월세+관리비 부족분.
- 검증: `tests.test_settlement_calendar`, `tests.test_payments_print`, 508-88 2018-09 인쇄·2026-08 화면 test client.
- 남은 정산 이슈는 `docs/TODO.md` (퇴실월 예외, `jungsan_det.manage_amt` 복사 버그, `calc_misu_amt` 과거단가, 일반관리 화면).

## 2026-09-09: 수금 수정 시 거래 시간 보존

- 대상: `payment_register.py`, `/payments/new` 수정 저장 분기(PR #68 후속 수정).
- 원인: 금액만 수정해도 `sukum_dt`를 수금일의 `00:00:00`으로 덮어썼다.
- 수정: UPDATE의 CASE 조건으로 날짜가 같으면 DB의 기존 `sukum_dt`를 보존한다. 사용자가 날짜를 변경한 경우에만 변경 날짜의 자정으로 저장하는 기존 동작을 유지한다.
- 검증: 저장 호출을 mock 처리한 POST 흐름에서 SQL과 인자 개수를 확인하고, 실제 SET 식을 MariaDB의 읽기 전용 SELECT로 평가했다. 금액 수정, 같은 날짜의 호실 변경은 `14:23:45` 보존, 날짜 변경은 새 날짜 자정으로 확인했다. Python 컴파일 및 `tests.test_tenant_form` 2개 통과.
- 실제 수금자료 UPDATE는 실행하지 않았다. 이미 과거 수정으로 소실된 거래 시간은 이번 변경으로 복구되지 않는다.
- 사용자 지시: 검증이 끝난 변경은 바로 커밋하고 인수인계 기록을 남긴다.

## 2026-09-09: 월별 정산서 조회 속도 확인

- 대상 URL: `/jungsan/list?q=1&year=2026&month=5`
- 현재 코드에서 test client로 3회 측정: `0.246초`, `0.203초`, `0.189초` (모두 HTTP 200).
- 따라서 월별 정산서 목록은 현재 약 0.2초 수준으로 개선되어 있다. 이전에 측정된 50~60초 결과는 개선 전 코드 또는 오래 실행된 서버 프로세스 기준으로 판단한다.
- 페이지 단위 계산 최적화 관련 커밋이 이미 반영되어 있으므로, 이후 성능 확인 시 반드시 최신 서버를 재시작하고 같은 URL로 재측정한다.

## 현재 작업: 입금파일 자동반영 CSRF 안정화

입금파일 자동반영 화면은 `sukum_import.py` 라우트와 다음 템플릿을 사용한다.

- `templates/payments_import.html`: 파일 업로드, 자동반영, 빠른 제외
- `templates/payments_import_matches.html`: 입금자/계좌 매칭 규칙 등록·삭제
- `templates/payments_import_exclude.html`: 제외 키워드 등록·삭제

앱은 Flask-WTF가 아니라 `app.py`의 자체 CSRF 검증을 사용한다. 모든 POST 요청은 세션의 `csrf_token`과 폼 또는 `X-CSRF-Token` 헤더의 값이 일치해야 한다. 템플릿에서는 `{{ csrf_token }}` 변수를 사용한다. `form.hidden_tag()`나 `csrf_token()` 함수는 사용하지 않는다.

## 이번 변경

간헐적인 `CSRF token validation failed`를 막기 위해 동적·인라인 POST 폼에도 토큰을 직접 넣었다.

- `templates/payments_import.html`
  - `exclude-quick-form`에 토큰 추가
  - `apply-form`에 토큰 추가
- `templates/payments_import_matches.html`
  - 매칭 규칙 등록/수정 폼에 토큰 추가
  - 목록의 삭제 폼에 토큰 추가
- `templates/payments_import_exclude.html`
  - 제외 키워드 등록/수정 폼에 토큰 추가
  - 목록의 삭제 폼에 토큰 추가

기본 업로드 폼에는 이미 토큰이 있었으므로 중복 추가하지 않았다. `base.html`에도 POST 폼 자동 주입이 있지만, 화면 일부가 동적으로 교체되는 경우를 고려해 각 폼에 직접 토큰을 넣는 방식으로 보강했다.

## 아직 남은 입금파일 TODO

- 한현승처럼 동일 이름으로 여러 호실을 쓰는 입금 분류 기준 확정
- 김호진/행복요양병원 적요가 잘리는 원인 확인
- 세입자명과 입금자명 불일치 사유 표시
- 농협 외 은행 파일 형식 지원
- 반영 완료 자료 잠금
- 1139-4 외 건물 실사용 테스트

## 작업 규칙

- 커밋은 사용자가 명시적으로 요청할 때만 한다.
- 인쇄 전용 템플릿/레이아웃은 별도 요청 없이는 변경하지 않는다.
- 변경 후 전체 Python 컴파일과 관련 템플릿 로드를 확인한다.
- `_check202.py`는 입금파일 데이터를 확인하기 위한 임시 진단 파일이며 기능 코드가 아니다.

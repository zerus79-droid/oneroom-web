# 할 일 (TODO, 2026-09-10 기준)

> 2026-09-03 문서(`docs/TODO.md`)는 9/5 커밋에서 설명 문구는 그대로 두고
> 체크박스만 전부 `[x]`로 바뀌었음(실제 수정 없이 완료 표시만 됨).
> 아래 한 일/할 일은 코드 대조와 2026-09-09~10 작업 기준이다.

## 한 일 (2026-09-09~10)

- [x] **공용비용 별도 테이블 폐기** — `bd01_common_cost`는 쓰지 않음. 건물 공용항목은 수리 `bd05_suri` 호수 **공용**, `YEAR(suri_dt)=1000` 마스터. 월정산 때 그달 말일로 복사해서 청구 누락을 막음. 정산 수리합은 `YEAR>1000`만.
- [x] **수금 목록/인쇄/엑셀을 XP처럼** — 칸: 수금일자/번지/호수/성명/입주일자/실수금액/대체금액/보증·예치/**임+관**. 종류 01은 전표 전액 한 칸(계약 월세·관리비로 쪼개지 않음). 주소+호수만 있으면 현재 입주 이력으로 고정.
- [x] **월정산 대체를 XP 결산현황에 맞춤** — 책임관리 입금액=월세(관리비 제외). 대체 호 입금액도 월세, 관리지시 `(대체)`, 미수는 대체로 안 깎음. 하단 **임대료대체 = 미수 합계**. 508-88 2018-09 인쇄가 XP PDF와 같음(101 입금 260,000/(대체)/미수 330,000, 임대료대체 1,590,000).
- [x] 103호(508-88 박연석) 달력월 01이 40만 미만이던 달 말일 대체 5건 입력(2022-12 4만 부족은 넣지 않음). DB 직접 입력, 코드 변경 아님.

**정산서 (책임관리/일반관리)**
- [ ] **XP 퇴실월 선불·후불 예외 검증** — 기본 양수/음수 일할 계산은 구현했지만, `후불 등록+실제 선입금`, `선불+실입 없음+대체`, `선불+미입금` 조합은 미확정. `docs/xp-settlement-logic.md`의 근거·검증표를 따라 실제 사례와 XP 결과로 확인할 것.
- [ ] **과거 계약금액 변경 적용일 보정 화면** — 2026-08-28부터 입주 수정 저장 시 `bd03_terms_hist`에 자동 기록하고 퇴실정산이 기간별 계산함. 그 전에 이미 덮어쓴 임대료·관리비·보증금 변경은 원자료가 없어 자동 복원하지 않음. 장부로 확인한 과거 적용일과 금액을 별도로 입력·수정하는 화면 필요. (코드 확인: `utils.py`에 `bd03_terms_hist` 참조는 있으나 별도 입력/수정 화면은 없음)
- [ ] **일반관리 전용 정산 화면 신규 제작** — `jungsan.py`는 책임관리 구조(당월지급액)만 지원. 일반관리는 컬럼(입금일/미납금)·계산(관리비+수리비+기타 실비를 건물주에게 청구, "수익"으로 마무리)이 근본적으로 다름 — 1584-8 PDF 형식 참고. **코드 재확인: 여전히 설계만 있고 "입금일/미납금" 컬럼이나 별도 화면 코드 없음.** 배경: `docs/modernization.md`.
- [ ] `jungsan_det`(호실별 정산 상세) 관리비 필드가 임대료를 복사해 잘못 저장되는 표시 버그 — 합계(`jungsan_m.manage_tot`)는 정상, 상세 화면만 틀림. **코드 재확인: 수정된 흔적 못 찾음. 안 고쳐진 것으로 보임.**
- [ ] `utils.calc_misu_amt` 계산값이 실제 결산서 PDF의 미수금이랑 다름을 발견함(예: 강석원 990,000원 vs 재계산 0원). 오래된 세입자(10년+)일수록 "총 개월수 × 현재 임대료"로 계산해서 옛날에 임대료가 더 쌌던 기간까지 지금 임대료 기준으로 소급 계산 — 그래서 실제보다 낮게(또는 0으로) 나오는 게 원인. **코드 재확인: `calc_misu_amt` 함수가 지금도 `(현재 rent_amt+manage_amt) × 경과월수 − 실입금` 그대로임. 버그 그대로 존재.** `misu.py`, `sukum_import.py`의 "확인필요" 경고도 이 함수에 의존 — 검증 필요.

**건물관리**
- [ ] `bd01.mgmt_gb`(관리형태) 필드는 추가했지만 자동 backfill(man_cost>0 추정)만 하고 508-88·1584-8 두 곳만 실제 확인함 — 나머지 건물도 책임관리/일반관리 맞는지 확인 필요. (필드/정규화 코드 자체는 있음 — `building.py`. 실제 데이터 확인만 남음)
- [ ] 건물관리 정보 부족 논의(주차대수, 건물유형, 승강기 유무, 안전점검 일정, 사진/메모 등)는 얘기만 하고 필드 추가는 안 함 — 필요하면 이어서 진행.

**입금파일 자동반영 (`sukum_import.py`)**
- [x] 계좌번호 우선 매칭, 이름/호수 힌트 자동 매칭, 복수호실 확인필요 드롭박스, 제외 목록, 확인필요 드롭박스+우선순위 — 완료 (코드 확인됨).
- [ ] **한현승 이름으로 호실 2개 쓰는 경우 분류 방법** — 주인 확인 필요(데이터 확인 사항, 코드 이슈 아님).
- [ ] **김호진(행복요양병원 같은 적요가 중간에 잘려 보이는 문제** — 원인(원본 데이터 vs CSS vs 파싱) 확인 필요.
- [ ] **세입자 이름 vs 입금자 이름 확인 보강** — 이름 불일치 자체를 사유로 표시하는 기능 미구현.
- [ ] **농협 외 다른 은행 파일 형식 지원 (카카오뱅크 등)** — **코드 재확인: 농협 관련 처리만 있고 다른 은행 대응 코드 없음. 미구현.**
- [ ] **"확인된(반영된) 데이터는 함부로 수정 못하게" 잠금 기능** — **코드 재확인: `lock`/잠금 관련 코드 전혀 없음. 미구현.**
- [ ] 1139-4 외 다른 건물로도 실사용 테스트 더 필요.

**인터페이스 현대화**
- [ ] `home.html`만 스타일 1차 적용됨 — 나머지 화면들(건물/입주자/정산/퇴실/수리 등)은 아직 레거시 그대로. 화면별로 범위를 정하고 진행 필요. 배경: `docs/modernization.md`.

**저장소**
- [x] 원격 저장소(GitHub) 연결 완료 — `https://github.com/zerus79-droid/oneroom-web.git`.
  - ⚠️ 단, GitHub에 `zerus79-droid-didactic-dollop`이라는 별도 브랜치(다중 파일 입금 가져오기 기능, 9/3일 시점 기준 작업)가 master와 병합 안 된 채 남아있음 — 필요 여부 판단 후 병합하거나 삭제 정리 필요.

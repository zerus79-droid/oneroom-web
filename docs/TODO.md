# 할 일 (TODO, 2026-08-18 기준)

작업 시작 전에만 읽으면 됨. 항상 필요한 규칙은 루트 `AGENTS.md` 참고.

**정산서 (책임관리/일반관리)**
- [ ] **일반관리 전용 정산 화면 신규 제작** — `jungsan.py`는 책임관리 구조(당월지급액)만 지원. 일반관리는 컬럼(입금일/미납금)·계산(관리비+수리비+기타 실비를 건물주에게 청구, "수익"으로 마무리)이 근본적으로 다름 — 1584-8 PDF 형식 참고. 아직 설계만 하고 코드는 없음. 배경: `docs/modernization.md`.
- [ ] `jungsan_det`(호실별 정산 상세) 관리비 필드가 임대료를 복사해 잘못 저장되는 표시 버그 — 합계(`jungsan_m.manage_tot`)는 정상, 상세 화면만 틀림. 원인 파악만 하고 아직 안 고침.
- [ ] `utils.calc_misu_amt` 계산값이 실제 결산서 PDF의 미수금이랑 다름을 발견함(예: 강석원 990,000원 vs 재계산 0원). 오래된 세입자(10년+)일수록 "총 개월수 × 현재 임대료"로 계산해서 옛날에 임대료가 더 쌌던 기간까지 지금 임대료 기준으로 소급 계산 — 그래서 실제보다 낮게(또는 0으로) 나오는 게 원인일 가능성이 큼(가설, 미검증). `misu.py`(미수금 조회 화면)도 이 함수를 씀. `sukum_import.py`의 "확인필요" 금액 경고(미수금 감안)도 이 함수에 의존해서 실제로 오탐 발생 확인함(2026-08-18, 한현승 건) — 검증 필요.

**건물관리**
- [ ] `bd01.mgmt_gb`(관리형태) 필드는 추가했지만 자동 backfill(man_cost>0 추정)만 하고 508-88·1584-8 두 곳만 실제 확인함 — 나머지 건물도 책임관리/일반관리 맞는지 확인 필요.
- [ ] 건물관리 정보 부족 논의(주차대수, 건물유형, 승강기 유무, 안전점검 일정, 사진/메모 등)는 얘기만 하고 필드 추가는 안 함 — 필요하면 이어서 진행.

**입금파일 자동반영 (`sukum_import.py`, 2026-08-18 대부분 작업)**
- [x] 계좌번호 우선 매칭, 이름/호수(N호·N층·숫자만+금액대조) 힌트로 자동 매칭, 복수호실 세입자 확인필요 드롭박스, 제외 목록(별도 페이지), 확인필요(금액이상/복수호실 둘 다) 드롭박스+우선순위 반영 — 완료.
- [ ] **한현승 이름으로 호실 2개 쓰는 경우 분류 방법** — DB엔 "한현승" 이름으로 현재 입주자가 B01호 1곳만 있음(다른 호실은 다른 이름으로 등록됐거나 미등록 추정). 실제로 몇 호실을 쓰는지, 어떻게 분류·등록해야 하는지 주인 확인 필요.
- [ ] **김호진(행복요양병원 같은 적요가 중간에 잘려 보이는 문제** — "김호진(행복요"처럼 뒤가 짤려 보임. 은행파일 원본 컬럼 자체가 짧은 건지, 화면(적요 칸 CSS 말줄임)이나 파싱 단계에서 잘리는 건지 원인 확인 필요.
- [ ] **세입자 이름 vs 입금자 이름 확인 보강** — 606호 "송민채잔금(606호" 사례처럼 실제 세입자(김민경)랑 적요 이름(송민채)이 다른데 호수 힌트로만 매칭되는 경우, 이름이 다르다는 걸 더 명확히 표시/경고하면 좋겠음(지금은 "확인필요(금액)"로만 걸림, 이름 불일치 자체를 사유로 표시 안 함).
- [ ] 농협 외 다른 은행 파일 형식 지원 (카카오뱅크 등 — 헤더 컬럼명 다를 수 있음, 실제 파일로 검증 필요).
- [ ] "확인된(반영된) 데이터는 함부로 수정 못하게" 잠금 기능 — 논의만 하고 미구현. 지금은 파일로 반영한 항목도 수동 입력이랑 수정 권한 동일.
- [ ] 1139-4 외 다른 건물로도 실사용 테스트 더 필요.

**인터페이스 현대화**
- [ ] `home.html`만 집사 스타일 1차 적용됨 — 나머지 화면들(건물/입주자/정산/퇴실/수리 등)은 아직 레거시 그대로. 화면별로 "레거시 유지 vs 현대화 허용" 범위를 하나씩 정하고 진행해야 함. 배경: `docs/modernization.md`.

**저장소**
- [ ] **원격 저장소(GitHub 등) 연결 보류 중 (2026-08-18)** — 지금은 로컬 git만 씀 (`git remote -v` 비어있음). 나중에 필요해지면 원격 만들고 연결.
